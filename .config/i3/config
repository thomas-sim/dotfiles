# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout some time, delete
# this file and re-run i3-config-wizard(1).
#

# i3 config file (v4)
#
# Please see https://i3wm.org/docs/userguide.html for a complete reference!

# WALLPAPER SETTINGS
# exec nitrogen --restore
set $wallpaper_command feh --randomize --bg-scale ~/Sync/Images/Wallpapers/* --randomize --bg-scale ~/Sync/Images/Wallpapers/*
exec --no-startup-id $wallpaper_command
bindsym $mod+Shift+w exec $wallpaper_command
# go double screen if connected
exec_always --no-startup-id xrandr --output DP-1 --right-of eDP-1 --auto

# PICOM COMPOSITOR
exec --no-startup-id picom -b
bindsym $mod+Ctrl+w exec ~/bin/picom-toggle.sh

## AUTOSTART APPLETS
exec --no-startup-id redshift-gtk
exec --no-startup-id nm-appet
exec --no-startup-id blueman-applet
# exec screenrotator
# exec TogglDesktop.sh
# exec --no-startup-id pa-applet
# Other applications
exec --no-startup-id /usr/lib/polkit-gnome/polkit-gnome-authentication-agent-1
exec --no-startup-id xfce4-power-manager

### Variables
# Logo key. Use Mod1 for Alt.
set $mod Mod4
# Home row direction keys, like vim
set $left h
set $down j
set $up k
set $right l
# Your preferred terminal emulator
set $term i3-sensible-terminal
# Your preferred application launcher
set $menu dmenu_run
set $other_menu ulauncher --no-window-shadow
set $print_command flameshot gui

# Font for window titles. Will also be used by the bar unless a different font
# is used in the bar {} block below.
font pango:Noto Sans 7

# This font is widely installed, provides lots of unicode glyphs, right-to-left
# text rendering and scalability on retina/hidpi displays (thanks to pango).
#font pango:DejaVu Sans Mono 8




### Key bindings
#
# Basics:
#
    # start a terminal
    bindsym $mod+Return exec $term
    bindsym $mod+n exec --no-startup-id ~/bin/open-note

    bindsym Print exec $print_command

    # kill focused window
    bindsym $mod+Shift+q kill

    # start your launcher
    bindsym $mod+Shift+d exec $other_menu
    bindsym $mod+d exec $menu

    # Drag floating windows by holding down $mod and left mouse button.
    # Resize them with right mouse button + $mod.
    # Despite the name, also works for non-floating windows.
    # Change normal to inverse to use left mouse button for resizing and right
    # mouse button for dragging.
    floating_modifier $mod

#
# Moving around:
#
    # Move your focus around
    bindsym $mod+$left focus left
    bindsym $mod+$down focus down
    bindsym $mod+$up focus up
    bindsym $mod+$right focus right
    # or use $mod+[up|down|left|right]
    bindsym $mod+Left focus left
    bindsym $mod+Down focus down
    bindsym $mod+Up focus up
    bindsym $mod+Right focus right

    # _move_ the focused window with the same, but add Shift
    bindsym $mod+Shift+$left move left
    bindsym $mod+Shift+$down move down
    bindsym $mod+Shift+$up move up
    bindsym $mod+Shift+$right move right
    # ditto, with arrow keys
    bindsym $mod+Shift+Left move left
    bindsym $mod+Shift+Down move down
    bindsym $mod+Shift+Up move up
    bindsym $mod+Shift+Right move right

#
# Workspaces:
#
set $monitor_main eDP-1
set $monitor_aux DP-1

workspace 0 output $monitor_main
workspace 1 output $monitor_main
workspace 2 output $monitor_main
workspace 3 output $monitor_main
workspace 4 output $monitor_main
workspace 5 output $monitor_main
workspace 6 output $monitor_main
workspace 7 output $monitor_aux
workspace 8 output $monitor_aux
workspace 9 output $monitor_aux
workspace 10 output $monitor_aux
workspace 11 output $monitor_aux
workspace 12 output $monitor_aux
workspace 13 output $monitor_aux
workspace 14 output $monitor_aux

# move workspace to left and right monitors
bindsym $mod+Shift+Prior move workspace to output left
bindsym $mod+Shift+Next move workspace to output right

    # switch to workspace
    bindsym $mod+twosuperior workspace number 0
    bindsym $mod+ampersand workspace number 1
    bindsym $mod+eacute workspace number 2
    bindsym $mod+quotedbl workspace number 3
    bindsym $mod+apostrophe workspace number 4
    bindsym $mod+parenleft workspace number 5
    bindsym $mod+minus workspace number 6
    bindsym $mod+egrave workspace number 7
    bindsym $mod+underscore workspace number 8
    bindsym $mod+ccedilla workspace number 9
    bindsym $mod+agrave workspace number 10
    bindsym $mod+parenright workspace number 11
    bindsym $mod+equal workspace number 12
    bindsym $mod+BackSpace workspace number 13

    workspace_auto_back_and_forth yes
    bindsym $mod+b workspace back_and_forth

    # move focused container to workspace
    bindsym $mod+Shift+twosuperior move container to workspace number 0; workspace number 0
    bindsym $mod+Shift+ampersand move container to workspace number 1; workspace number 1
    bindsym $mod+Shift+eacute move container to workspace number 2; workspace number 2
    bindsym $mod+Shift+quotedbl move container to workspace number 3; workspace number 3
    bindsym $mod+Shift+apostrophe move container to workspace number 4; workspace number 4
    bindsym $mod+Shift+parenleft move container to workspace number 5; workspace number 5
    bindsym $mod+Shift+minus move container to workspace number 6; workspace number 6
    bindsym $mod+Shift+egrave move container to workspace number 7; workspace number 7
    bindsym $mod+Shift+underscore move container to workspace number 8; workspace number 8
    bindsym $mod+Shift+ccedilla move container to workspace number 9; workspace number 9
    bindsym $mod+Shift+agrave move container to workspace number 10; workspace number 10
    bindsym $mod+Shift+parenright move container to workspace number 11; workspace number 11
    bindsym $mod+Shift+equal move container to workspace number 12; workspace number 12
    bindsym $mod+Shift+BackSpace move container to workspace number 13; workspace number 13

    bindsym $mod+Ctrl+h workspace prev
    bindsym $mod+Ctrl+l workspace next
    # Note: workspaces can have any name you want, not just numbers.
    # We just use 1-10 as the default.
#
# Layout stuff:
#
    # You can "split" the current object of your focus with
    # $mod+b or $mod+v, for horizontal and vertical splits
    # respectively.
    bindsym $mod+v splitv

    # Switch the current container between different layout styles
    bindsym $mod+s layout stacking
    bindsym $mod+Shift+e layout tabbed
    bindsym $mod+e layout toggle split

    # Make the current focus fullscreen
    bindsym $mod+f fullscreen

    # Toggle the current focus between tiling and floating mode
    bindsym $mod+Shift+space floating toggle

    # Swap focus between the tiling area and the floating area
    bindsym $mod+space focus mode_toggle

    # move focus to the parent container
    bindsym $mod+a focus parent

#
# Scratchpad:
#
    # Sway has a "scratchpad", which is a bag of holding for windows.
    # You can send windows there and get them back later.

    # Move the currently focused window to the scratchpad
    bindsym $mod+Shift+Escape move scratchpad

    # Show the next scratchpad window or hide the focused scratchpad window.
    # If there are multiple scratchpad windows, this command cycles through them.
    bindsym $mod+Escape scratchpad show

    bindsym $mod+Shift+s sticky toggle
# reload the configuration file
bindsym $mod+Shift+c reload
# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+r restart

# resize window (you can also use the mouse for that)
mode "resize" {
        # These bindings trigger as soon as you enter the resize mode

        # Pressing left will shrink the window’s width.
        # Pressing right will grow the window’s width.
        # Pressing up will shrink the window’s height.
        # Pressing down will grow the window’s height.
        bindsym j resize shrink width 10 px or 10 ppt
        bindsym k resize grow height 10 px or 10 ppt
        bindsym l resize shrink height 10 px or 10 ppt
        bindsym m resize grow width 10 px or 10 ppt

        # same bindings, but for the arrow keys
        bindsym Left resize shrink width 10 px or 10 ppt
        bindsym Down resize grow height 10 px or 10 ppt
        bindsym Up resize shrink height 10 px or 10 ppt
        bindsym Right resize grow width 10 px or 10 ppt

        # back to normal: Enter or Escape or $mod+r
        bindsym Return mode "default"
        bindsym Escape mode "default"
        bindsym $mod+r mode "default"
}

# Set shut down, restart and locking features
bindsym $mod+Delete mode "$mode_system"
set $mode_system (l)ock, (e)xit, switch_(u)ser, (s)uspend, (h)ibernate, (r)eboot, (Shift+s)hutdown
mode "$mode_system" {
    bindsym l exec --no-startup-id i3exit lock, mode "default"
    bindsym s exec --no-startup-id i3exit suspend, mode "default"
    bindsym u exec --no-startup-id i3exit switch_user, mode "default"
    bindsym e exec --no-startup-id i3exit logout, mode "default"
    bindsym h exec --no-startup-id i3exit hibernate, mode "default"
    bindsym r exec --no-startup-id i3exit reboot, mode "default"
    bindsym Shift+s exec --no-startup-id i3exit shutdown, mode "default"

    # exit system mode: "Enter" or "Escape"
    bindsym Return mode "default"
    bindsym Escape mode "default"
}

bindsym $mod+r mode "resize"

# Start i3bar to display a workspace bar (plus the system information i3status
# finds out, if available)
bar {
    # status_command i3status
    i3bar_command $HOME/.config/polybar/launch.sh

    position top
    colors {
        background #303030
        statusline #BCBCBC
        separator #5F8787

        # class                 border  backgr. text    indicator child_border
        # focused_workspace  #5F8787 #ff8700 #262626
        # active_workspace   #5F8787 #5F87AF #262626
        # inactive_workspace #5F8787 #262626 #BCBCBC
        # urgent_workspace   #5F8787 #AF5F5F #262626
    }
}

# class                 border  backgr. text    indicator child_border
client.focused          #d6a164 #d6a164 #000000 #d6a164   #d6a164
client.focused_inactive #72624f #72624f #F8F8F2 #72624f   #72624f
client.unfocused        #282A36 #282A36 #BFBFBF #282A36   #282A36
client.urgent           #44475A #FF5555 #F8F8F2 #FF5555   #FF5555
client.placeholder      #282A36 #282A36 #F8F8F2 #282A36   #282A36

client.background       #F8F8F2

new_window pixel 2
default_floating_border pixel 1

hide_edge_borders smart

bindsym XF86MonBrightnessDown exec brightnessctl set 1%-
bindsym XF86MonBrightnessUp exec brightnessctl set +1%
# Multimedia Keys
# SOUND
set $sink `ponymix defaults | grep sink | cut -d' ' -f2 | cut -f1 -d:`
bindsym XF86AudioRaiseVolume exec --no-startup-id pactl set-sink-volume $sink +5%
bindsym XF86AudioLowerVolume exec --no-startup-id pactl set-sink-volume $sink -5%
bindsym XF86AudioMute exec --no-startup-id pactl set-sink-mute $sink toggle
bindsym $mod+XF86AudioRaiseVolume exec ~/bin/rofi-pulse.sh sink
bindsym $mod+XF86AudioLowerVolume exec ~/bin/rofi-pulse.sh source
# pause / play / next / previous
bindsym XF86AudioPlay exec playerctl play-pause
bindsym XF86AudioNext exec playerctl next
bindsym XF86AudioPrev exec playerctl previous

for_window [title="Mixer"] floating disable
for_window [title="Mixer"] border pixel 1px
# browser pop-ups
for_window [window_role="pop-up"] floating enable

for_window [class="Toggl Desktop"] floating enable
for_window [class="Toggl Desktop"] sticky enable

for_window [title="Picture-in-Picture"] sticky enable

bindsym $mod+F12 exec i3-scratchpad -m t -t pavucontrol -t 3
bindsym $mod+F1 exec i3-scratchpad -t go-for-it
bindsym $mod+F2 exec i3-scratchpad -t keepassxc
bindsym $mod+F3 exec i3-scratchpad -d75%x75% -t clementine

# rename workspaces
bindsym $mod+asterisk exec i3-input -F "rename workspace to "$(i3-msg -t get_workspaces | jq -r "map(select(.focused))[0].num"):%s"" -P "Rename workspace with number :"
bindsym $mod+Shift+asterisk exec i3-input -F 'rename workspace to "%s"' -P 'Rename workspace :'
# clear workspace name
bindsym $mod+Ctrl+asterisk exec i3-msg "rename workspace to "$(i3-msg -t get_workspaces | jq -r "map(select(.focused))[0].num")""

